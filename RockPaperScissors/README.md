Small CLI-based game of Rock-Paper-Scissors using random decision making.

Potential optimizations:
   - Only import np.random.randint to remove dependencies
   - Add an LSTM Neural Network to increase difficulty.
